export default function Page() {
  return (
    <p>
      Dashboard Page : One benefit of using layouts in Next.js is that on
      navigation, only the page components update while the layout won&apos;t
      re-render. This is called partial rendering:
    </p>
  );
}
